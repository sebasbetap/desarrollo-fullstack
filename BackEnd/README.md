# Desarrollo WrapperCoin

Ejercicio Fullstack Wrapper de BraveNewCoin para Bancolombia

# How to Run BackEnd API

1. Install all packages inside Backend folder with `npm install`
2. Run `nodemon backend/api/index.js`